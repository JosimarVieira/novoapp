# Copie para server/.env e preencha. O .env e lido automaticamente pelo Quarkus
# a partir do diretorio de onde voce roda o mvn, e esta no .gitignore.
#
# NUNCA commite o .env preenchido.

NOVOAPP_DB_URL=jdbc:postgresql://localhost:5432/novoapp
NOVOAPP_DB_ADMIN_USER=postgres
NOVOAPP_DB_ADMIN_PASSWORD=postgres
NOVOAPP_DB_RUNTIME_PASSWORD=

# BotFather
TELEGRAM_BOT_TOKEN=
# head -c 32 /dev/urandom | base64 | tr -d '=+/'
TELEGRAM_WEBHOOK_SECRET=

# console.mistral.ai (ADR-0009)
MISTRAL_API_KEY=
